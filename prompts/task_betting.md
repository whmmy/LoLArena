# 任务：对一场英雄联盟系列赛的盘口给出下注建议

下面是本场比赛的基本信息、**已开盘的赔率**，以及**本场所有预测模型的分析结论**（作为你的情报参考）。请基于这些信息，按价值投注的方法论，先逐盘口写出中文价值分析叙述，最后输出一张固定的 markdown 推荐表。

---

## 比赛信息

{{match_context}}

---

## 盘口赔率（赔率为 0 或缺失表示未开盘，不要纳入分析）

{{odds_block}}

---

## 参考分析（本场所有预测模型的结论）

{{reference_analyses}}

---

## 分析要求

1. 先把"所有模型对胜负/比分/总局数的共识"提炼出来（例如 5 个模型平均胜率、大家最倾向的比分），作为你判断"模型概率"的依据。
2. 对每一个**已开盘**的盘口，计算其隐含概率（建议按 95% 返还率折算），与模型共识概率对比，判断是否存在价值。
3. 用简体中文逐盘口写出价值分析叙述（每段包含：该盘口模型共识概率、隐含概率、是否值得下注、主要风险）。
4. 最后输出推荐表，**严格**使用下面的表头（程序会解析，请勿改动列名与顺序）：

`| 市场 | 选择 | 赔率 | 模型概率 | 隐含概率 | 价值 | 信心 | 理由 |`

- 只把价值=`有`的投注项列入推荐表。
- 价值列只填 `有` 或 `无`；信心列填 `高` / `中` / `低`。
- 若全场无价值盘口，仍输出表头，并附一行注明"本场无价值盘口"。

记住：先叙述、后表格；只输出内容本身，不要输出 markdown 围栏包裹整段。
